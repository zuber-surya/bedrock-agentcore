# Local / docs — live defaults (do not put secrets here)
# Use `aws configure` / SSO for credentials. Optional keys only in gitignored `.env`.
# Account: 390403887579 (IAM user zuber), region us-east-1
USE_BEDROCK=true
BEDROCK_MODEL_ID=us.anthropic.claude-haiku-4-5-20251001-v1:0
BEDROCK_FALLBACK_MODEL_ID=us.amazon.nova-lite-v1:0
AWS_DEFAULT_REGION=us-east-1
